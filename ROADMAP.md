# FabGuard AI 로드맵

FabGuard의 우선순위는 반도체 제조 위험탐지 본체의 완성도와 검증이다. 외부 오픈소스 연계는
본체를 대체하는 새 프로젝트가 아니라, FabGuard에서 검증한 산업 데이터 기술을 다른 운영환경과
도메인에 단계적으로 이전하는 과정으로 정의한다.

> **기술 서사:** FabGuard 반도체 제조 위험탐지 → Fledge 산업 엣지 데이터 연동 →
> Solar Data Tools 태양광 설비 시계열로 확장

## 상태 표기

- **확정**: 현재 프로젝트가 지켜야 할 방향·불변조건 또는 소유자가 결정한 일정이다.
- **검토**: 외부 커뮤니티 논의와 기술 검증을 거쳐야 하며 구현을 약속하지 않는다.
- **보류**: 진입 조건을 만족하기 전에는 코드·종속성·배포 구성을 추가하지 않는다.

## 현재 구조 검토

| 경계 | 현재 상태 | 판단 |
|---|---|---|
| 데이터 입력 | `data.py`의 SECOM 로더·무결성 검사·시간 분할 | 데이터 계약은 강하지만 SECOM 형식에 특화되어 있다 |
| 전처리 | `TrainColumnFilter`와 scikit-learn Pipeline | 학습 구간에만 fit하는 누출 방지 경계가 명확해 재사용 가능성이 높다 |
| 추론 | 학습·선택·추론이 `experiment.py` 실행 흐름에 함께 있다 | V1 재현에는 적절하나 외부 호출용 얇은 추론 인터페이스는 아직 없다 |
| 정책/알림 | Top-K 정책은 평가 함수와 결과 파일로 표현된다 | 점검 정책과 모델 평가를 분리할 필요가 있으나 지금은 리팩터링하지 않는다 |
| UI | `reporting.py`가 JSON을 만들고 정적 웹이 이를 읽는다 | UI가 Python 모델을 직접 호출하지 않아 현재 결합도는 낮다 |

**결론:** 연계 계획은 가능하다. 기존 실험계약을 유지한 채 외부 입력 어댑터와 출력 정책을
코어 밖에 두면 된다. 다만 아직 두 번째 실제 입력원이 없으므로 추상화 계층을 미리 대규모로
구현하면 사용되지 않는 인터페이스가 될 위험이 있다.

## 목표 모듈 경계

향후 코드는 아래 방향으로만 의존하게 한다.

1. **입력 어댑터** — SECOM 파일 또는 Fledge reading을 정규화된 배치로 변환한다.
2. **데이터 품질·전처리** — 스키마 검사, 결측·상수·중복·이상값 처리를 학습/추론 계약에 맞게 수행한다.
3. **위험도 추론** — 입력 배치에서 `sample_id`, `event_time`, `risk_score`, 근거 범위를 반환한다.
4. **정책·알림** — 점검예산, 임계값, 드리프트 규칙을 적용하되 모델과 독립적으로 교체할 수 있게 한다.
5. **출력 계약** — UI·Fledge·향후 알림 서비스가 공통 결과를 소비한다.
6. **표현 계층** — 웹 UI는 출력 계약만 읽고 학습 코드나 원시 데이터 로더를 직접 참조하지 않는다.

각 외부 시스템의 데이터 형식·수명주기·오류 처리는 어댑터가 담당한다. FabGuard 코어는 Fledge나
Solar Data Tools 패키지를 import하지 않으며, 외부 연계가 V1의 데이터 해시, 시간 분할, 누출 방지,
재현성 및 UI 데이터계약을 변경하지 못하게 한다.

## 0단계 — FabGuard 본체 완성

### 확정사항

- 고정된 SECOM V1 실험계약과 원본 해시를 유지한다.
- 시간 홀드아웃, 학습 폴드 내부 전처리, 재현 명령과 결과 manifest를 유지한다.
- Phase 1의 비용 시나리오, bootstrap 구간, 확률 보정, 드리프트와 walk-forward 결과를 실제
  원본 데이터로 실행·검토한 뒤 공개 수치의 범위를 결정한다.
- UI에는 모델 결과와 운영 시뮬레이션, 아직 검증되지 않은 현장 KPI를 구분해 표시한다.

### 지금 적용할 최소 구조 원칙

- 새 입력원은 `data.py`에 조건문으로 누적하지 않고 별도 어댑터에서 정규화한다.
- 전처리기는 pandas 데이터 입력과 안정적인 feature name 계약을 유지한다.
- 추론 결과와 UI JSON 필드는 명시적으로 버전 관리하고, 필드 변경 시 계약 테스트를 추가한다.
- 점검예산·알림 기준은 모델 학습 파라미터와 분리한다.
- 외부 어댑터가 실패해도 V1 재현 명령과 정본 결과가 변하지 않게 한다.

### 지금 하지 않는 일

- 패키지 전면 재배치, 플러그인 프레임워크 도입, 실시간 스트리밍 런타임 구현
- 검증되지 않은 범용 `BaseAdapter`·`BaseModel` 계층의 선제적 추가
- Fledge 또는 Solar Data Tools 종속성 추가

## 1차 연계 — Fledge

현재 준비 상태: **외부 종속성 없는 reading 정규화 계약과 단위 테스트 구현 완료. 실제 Fledge
플러그인·런타임 연동은 아직 시작하지 않음.** 자세한 경계는
[`docs/FLEDGE_ADAPTER_CONTRACT.md`](docs/FLEDGE_ADAPTER_CONTRACT.md)에 기록한다. 공식 커뮤니티
접촉 전 후보 범위와 기여 절차는
[`docs/FLEDGE_UPSTREAM_READINESS.md`](docs/FLEDGE_UPSTREAM_READINESS.md)를 따른다.

### 확정사항

- 현재 진행 중인 해양 프로젝트 2개를 마무리한 뒤 착수한다.
- FabGuard 본체는 독립적인 반도체 제조 AI 프로젝트로 유지한다.
- 개인 포크 구현에 그치지 않고 Fledge 공식 커뮤니티에서 문제와 범위를 먼저 논의한 뒤,
  이슈·PR 제출·리뷰 대응·병합까지 경험하는 것을 목표로 한다.
- 연동 작업은 FabGuard의 검증된 실험계약, 누출 방지, 재현성과 UI 출력 계약을 변경하지 않는다.

### 검토사항

- 데이터 품질검사, 결측·이상값 처리, 위험도 추론 중 어떤 기능이 Fledge의 filter plugin에
  가장 작고 독립적인 기여가 되는지 확인한다.
- 수집 단계의 변환인지, 송신 단계의 축약·보강인지에 따라 ingress/egress 위치를 결정한다.
- 상태를 가진 드리프트 감지가 Fledge의 배치·재시작·설정 변경 수명주기에서 안전한지 검증한다.
- Python 플러그인 관례, 테스트 방식, 라이선스와 maintainer가 선호하는 기여 단위를 먼저 확인한다.

### 현재 확보한 최소 구조

- `src/fabguard/integrations/fledge_contract.py`에 외부 패키지를 import하지 않는 입력 정규화 경계를 둔다.
- malformed timestamp, 비수치 측정값, 필수 tag 누락과 중복 identity를 fail-closed로 차단한다.
- 결과 열을 `sample_id`, `asset_code`, `event_time`, `measurement__*`로 고정해 향후 추론·정책
  계층과 Fledge 수명주기를 분리한다.
- 이 코드는 연계 가능성을 검증하는 독자 구현이며 Fledge 호환 또는 현장 배포 완료를 뜻하지 않는다.
- 로컬 운영 검증 하니스의 오류 격리, 재시작 상태, 지연·단절, 부하 측정, 드리프트·알림 계약은
  [`docs/FLEDGE_OPERATIONAL_VALIDATION.md`](docs/FLEDGE_OPERATIONAL_VALIDATION.md)에 기록한다.

### 진입 조건

1. FabGuard Phase 1 결과와 출력 계약이 동결되어 있다.
2. 범용 후보 기능에 순수 함수 단위 테스트와 입출력 예제가 있다.
3. Fledge 최신 공개 이슈·기여 지침·플러그인 예제를 확인했다.
4. 구현 전 upstream 이슈에서 문제, 범위, 테스트 방법에 대한 피드백을 받았다.

## 2차 연계 — Solar Data Tools

### 확정사항

- 참여 목표 시점은 **2027년 여름~초가을**이다.
- 지금은 Solar Data Tools 코드를 FabGuard에 복사하거나 런타임 종속성으로 추가하지 않는다.
- FabGuard/Fledge에서 축적한 결측 처리, 데이터 품질검사, 이상탐지, 드리프트 감지,
  테스트·재현성 기술을 태양광 발전 시계열 문제로 이전할 수 있도록 모듈 경계를 유지한다.
- 2027년 실제 활동, 공개 이슈와 기여 지침을 다시 확인한 뒤 문서·테스트·작은 버그 수정부터
  시작하고 공식 PR 병합을 목표로 한다.

### 검토사항

- FabGuard의 범용 코드 이전이 실제 upstream 요구와 맞는지, 아니면 Solar Data Tools의 기존
  데이터 품질 파이프라인을 테스트·문서로 보강하는 편이 더 가치 있는지 확인한다.
- 반도체 생산 건 단위 위험순위와 태양광 시계열 품질분석의 목적·시간축·라벨 차이를 별도
  계약으로 다룬다. 같은 모델이나 지표를 그대로 재사용하지 않는다.
- 프로젝트 조직, maintainer 활동, 릴리스 상태와 라이선스를 참여 시점에 다시 확인한다.

### 진입 조건

1. Fledge 또는 동등한 외부 오픈소스에서 리뷰 대응 경험을 확보했다.
2. 시계열 결측·드리프트 기능이 도메인 독립 테스트로 검증되어 있다.
3. Solar Data Tools의 최신 로드맵·공개 이슈에서 실제 기여 수요를 확인했다.
4. 작은 문서·테스트 기여 후 maintainer 피드백을 반영해 코드 PR 범위를 정했다.

## 나중으로 미룰 구현

| 항목 | 미루는 이유 | 재검토 시점 |
|---|---|---|
| Fledge 전용 저장소·플러그인 패키지 | upstream과 기여 단위 합의가 먼저다 | Fledge 이슈 논의 후 |
| 실시간 배치·재시도·상태 저장 | 현재 FabGuard는 오프라인 실험이며 요구사항이 없다 | 플러그인 수명주기 확정 후 |
| 정책/알림 서비스 분리 | 실제 소비자와 SLA가 아직 없다 | Fledge 샘플 흐름 확보 후 |
| Solar Data Tools import·어댑터 | 2027년 upstream 요구가 미확정이다 | 참여 직전 활동성 점검 후 |
| 공통 산업 AI SDK | 두 도메인에서 검증되기 전에는 과잉 추상화다 | 두 번째 실제 연계 완료 후 |

## 외부 근거와 저작권 경계

- Fledge 공식 저장소와 아키텍처 문서에 따르면 filter는 데이터 스트림을 변환하는 plugin이며
  수집 또는 송신 파이프라인에 배치할 수 있다. 이 구조를 연계 가능성 판단에만 사용한다.
- Solar Data Tools는 태양광 발전·일사량 시계열의 입출력, 정제, 필터링과 분석을 제공하는
  오픈소스 Python 라이브러리다. 현재 공개 저장소와 공식 문서의 설명만 참고한다.
- 외부 프로젝트의 코드·그림·표·문구를 이 저장소에 복제하지 않는다. 실제 기여 시점에 각
  저장소의 라이선스, 기여 지침과 저작권 고지 요건을 다시 검토한다.

공식 참고 링크:

- [Fledge repository](https://github.com/fledge-iot/fledge)
- [Fledge architecture](https://fledge-iot.readthedocs.io/en/latest/fledge_architecture.html)
- [Solar Data Tools repository](https://github.com/NatLabRockies/solar-data-tools)
- [Solar Data Tools documentation](https://solar-data-tools.readthedocs.io/)
